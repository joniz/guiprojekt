<UserControl x:Class="guiprojekt.addReminder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="LightBlue">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />

        </Grid.RowDefinitions>
        <UniformGrid Grid.Row="0">
            <Label Content="Kom ihåg: " FontSize="20" FontStyle="Italic" />
            <TextBox Name="titleForReminder" ></TextBox>
        </UniformGrid>
        <DatePicker SelectedDate="{Binding Date, StringFormat={}{0:dd/MM/yyyy}}" Margin="0,35,0,40" Grid.Row="1" Grid.RowSpan="2"></DatePicker>
    </Grid>
</UserControl>
